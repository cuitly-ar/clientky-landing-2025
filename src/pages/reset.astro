---
import AuthPage from '../components/pages/AuthPage.astro';
import ComingSoon from '../components/ComingSoon.astro';
import { getLanguage } from '../i18n/utils';

const lang = getLanguage(Astro.url);
const showPrivatePage = import.meta.env.DEV;
const homeHref = lang === 'en' ? '/en/' : '/';
---

{showPrivatePage ? <AuthPage lang={lang} mode="reset" /> : <ComingSoon lang={lang} homeHref={homeHref} />}

