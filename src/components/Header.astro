---
import LanguageSelector from './LanguageSelector.astro';
import { getLanguage, t } from '../i18n/utils';

const lang = getLanguage(Astro.url);
const homeHref = lang === 'en' ? '/en/' : '/';
const loginEnabled = import.meta.env.DEV;
const loginHref = loginEnabled ? (lang === 'en' ? '/en/login' : '/login') : '#';
const pricingHref = '#precios';
---

<header class="fixed top-0 left-0 w-full z-50 bg-white/95 backdrop-blur-md shadow-sm transition-all duration-300">
  <div class="container mx-auto px-6 lg:px-20">
    <div class="flex justify-between items-center py-5">
      <!-- Logo -->
      <a href={homeHref} class="flex items-center gap-3 text-2xl font-bold text-primary hover:opacity-80 transition-opacity duration-300">
        <span class="logo-isotype" aria-hidden="true"></span>
        <span>Clientky<span class="text-gray-900">.</span></span>
      </a>
      
      <!-- Desktop Navigation -->
      <nav class="hidden md:flex items-center space-x-8" aria-label="Main navigation">
        <a href="#inicio" class="text-gray-900 font-medium hover:text-primary transition-colors duration-300 relative group">
          {t(lang, 'home')}
          <span class="absolute bottom-0 left-0 w-0 h-0.5 bg-primary group-hover:w-full transition-all duration-300"></span>
        </a>
        <a href="#instagram" class="text-gray-900 font-medium hover:text-primary transition-colors duration-300 relative group">
          {t(lang, 'instagram')}
          <span class="absolute bottom-0 left-0 w-0 h-0.5 bg-primary group-hover:w-full transition-all duration-300"></span>
        </a>
        <a href="#tiktok" class="text-gray-900 font-medium hover:text-primary transition-colors duration-300 relative group">
          {t(lang, 'tiktok')}
          <span class="absolute bottom-0 left-0 w-0 h-0.5 bg-primary group-hover:w-full transition-all duration-300"></span>
        </a>
        <a href="#precios" class="text-gray-900 font-medium hover:text-primary transition-colors duration-300 relative group">
          {t(lang, 'pricing')}
          <span class="absolute bottom-0 left-0 w-0 h-0.5 bg-primary group-hover:w-full transition-all duration-300"></span>
        </a>
        <a href="#recursos" class="text-gray-900 font-medium hover:text-primary transition-colors duration-300 relative group">
          {t(lang, 'resources')}
          <span class="absolute bottom-0 left-0 w-0 h-0.5 bg-primary group-hover:w-full transition-all duration-300"></span>
        </a>
      </nav>
      
      <!-- Auth Buttons -->
      <div class="hidden md:flex items-center space-x-4">
        <LanguageSelector />
        <a
          href={loginHref}
          aria-disabled={!loginEnabled}
          class={`text-gray-900 font-medium ${
            loginEnabled ? 'hover:text-primary transition-colors duration-300' : 'opacity-50 cursor-default pointer-events-none'
          }`}
        >
          {t(lang, 'login')}
        </a>
        <a href={pricingHref} class="bg-gradient-to-r from-primary to-secondary text-white px-6 py-3 rounded-xl font-semibold shadow-lg hover:scale-105 hover:shadow-xl transition-all duration-300 ease-in-out inline-block">
          {t(lang, 'getStarted')}
        </a>
      </div>
      
      <!-- Mobile Menu Button -->
      <button 
        id="mobile-menu-btn" 
        class="md:hidden text-gray-900 text-2xl focus:outline-none" 
        aria-label="Toggle mobile menu"
        aria-expanded="false"
      >
        ☰
      </button>
    </div>
  </div>
  
  <!-- Mobile Menu -->
  <div id="mobile-menu" class="hidden md:hidden bg-white border-t border-gray-200">
    <nav class="container mx-auto px-6 py-4 flex flex-col space-y-4">
      <a href="#inicio" class="text-gray-900 font-medium hover:text-primary transition-colors duration-300">{t(lang, 'home')}</a>
      <a href="#instagram" class="text-gray-900 font-medium hover:text-primary transition-colors duration-300">{t(lang, 'instagram')}</a>
      <a href="#tiktok" class="text-gray-900 font-medium hover:text-primary transition-colors duration-300">{t(lang, 'tiktok')}</a>
      <a href="#precios" class="text-gray-900 font-medium hover:text-primary transition-colors duration-300">{t(lang, 'pricing')}</a>
      <a href="#recursos" class="text-gray-900 font-medium hover:text-primary transition-colors duration-300">{t(lang, 'resources')}</a>
      <hr class="border-gray-200" />
      <div class="py-2">
        <LanguageSelector />
      </div>
      <a
        href={loginHref}
        aria-disabled={!loginEnabled}
        class={`text-gray-900 font-medium text-left ${
          loginEnabled ? 'hover:text-primary transition-colors duration-300' : 'opacity-50 cursor-default pointer-events-none'
        }`}
      >
        {t(lang, 'login')}
      </a>
      <a href={pricingHref} class="bg-gradient-to-r from-primary to-secondary text-white px-6 py-3 rounded-xl font-semibold shadow-lg text-center">
        {t(lang, 'getStarted')}
      </a>
    </nav>
  </div>
</header>

<script>
  // Mobile menu toggle
  const mobileMenuBtn = document.getElementById('mobile-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  
  if (mobileMenuBtn && mobileMenu) {
    mobileMenuBtn.addEventListener('click', () => {
      mobileMenu.classList.toggle('hidden');
      const isExpanded = mobileMenuBtn.getAttribute('aria-expanded') === 'true';
      mobileMenuBtn.setAttribute('aria-expanded', String(!isExpanded));
    });
  }

  // Smooth scroll
  document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e: Event) {
      e.preventDefault();
      const href = (this as HTMLAnchorElement).getAttribute('href');
      if (href) {
        const element = document.querySelector(href);
        if (element) {
          element.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
          // Close mobile menu after click
          mobileMenu?.classList.add('hidden');
          mobileMenuBtn?.setAttribute('aria-expanded', 'false');
        }
      }
    });
  });
</script>

<style>
  :global(.logo-isotype) {
    width: 36px;
    height: 36px;
    background-color: #7c3aed;
    mask: url('/logos/mesa-isotipo.png') center/contain no-repeat;
    -webkit-mask: url('/logos/mesa-isotipo.png') center/contain no-repeat;
    display: inline-block;
  }
</style>

