---
import { getLanguage, t } from '../../i18n/utils';
import BrandMarquee from '../BrandMarquee.astro';

const lang = getLanguage(Astro.url);
---

<div class="hidden lg:block lg:w-1/2 bg-gradient-to-br from-primary to-purple-700 text-white p-12">
  <!-- Trust Badges -->
  <div class="mb-8">
    <p class="text-xs font-semibold mb-4 opacity-90">{t(lang, 'trustedBy')}</p>
    <BrandMarquee variant="dark" />
  </div>
  
  <!-- Main Content -->
  <div class="space-y-8 mt-12">
    <div>
      <h2 class="text-3xl font-bold mb-4 leading-tight">
        {t(lang, 'unlock10x')}
      </h2>
      <div class="w-32 h-1 bg-white/30"></div>
    </div>
    
    <!-- Benefits -->
    <div class="space-y-6">
      <div class="flex gap-4">
        <div class="flex-shrink-0 w-14 h-14 bg-white/10 rounded-xl flex items-center justify-center text-3xl">
          🎯
        </div>
        <div>
          <h3 class="font-bold text-lg mb-2">{t(lang, 'provenGrowth')}</h3>
          <p class="text-sm opacity-90 leading-relaxed">
            {t(lang, 'provenGrowthDesc')}
          </p>
        </div>
      </div>
      
      <div class="flex gap-4">
        <div class="flex-shrink-0 w-14 h-14 bg-white/10 rounded-xl flex items-center justify-center text-3xl">
          📊
        </div>
        <div>
          <h3 class="font-bold text-lg mb-2">{t(lang, 'handsFree')}</h3>
          <p class="text-sm opacity-90 leading-relaxed">
            {t(lang, 'handsFreeDesc')}
          </p>
        </div>
      </div>
      
      <div class="flex gap-4">
        <div class="flex-shrink-0 w-14 h-14 bg-white/10 rounded-xl flex items-center justify-center text-3xl">
          📧
        </div>
        <div>
          <h3 class="font-bold text-lg mb-2">{t(lang, 'trackSuccess')}</h3>
          <p class="text-sm opacity-90 leading-relaxed">
            {t(lang, 'trackSuccessDesc')}
          </p>
        </div>
      </div>
    </div>
    
    <div class="pt-8">
      <p class="text-lg font-semibold">
        {t(lang, 'readyToGrow')}
      </p>
    </div>
  </div>
</div>



