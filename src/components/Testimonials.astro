---
import { getLanguage, t } from '../i18n/utils';

const lang = getLanguage(Astro.url);

const testimonials = [
  {
    content: "Clientky transformó completamente mi presencia en Instagram. En solo 3 meses, pasé de 5,000 a 50,000 seguidores reales y comprometidos.",
    author: "María José",
    role: "Influencer de Belleza",
    avatar: "MJ"
  },
  {
    content: "Como agencia de marketing, hemos probado muchas herramientas. Clientky es, con diferencia, la más efectiva y confiable para el crecimiento orgánico.",
    author: "Andrés Gómez",
    role: "Director de Agencia",
    avatar: "AG"
  },
  {
    content: "Antes de Clientky, estuve estancado en 3,000 seguidores durante meses. Ahora tengo 15,000 en TikTok y cada vídeo tiene un alcance constante.",
    author: "Nico Felich",
    role: "Influencer de TikTok",
    avatar: "NF"
  }
];
---

<section class="py-20 md:py-32 bg-white">
  <div class="container mx-auto px-6 lg:px-20">
    <!-- Section Title -->
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-gray-900 mb-6">
        {t(lang, 'testimonialsTitle')}
      </h2>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto">
        {t(lang, 'testimonialsSubtitle')}
      </p>
    </div>
    
    <!-- Testimonials Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {testimonials.map((testimonial) => (
        <div class="bg-white rounded-2xl p-8 shadow-lg hover:shadow-2xl transition-all duration-300 ease-in-out">
          <div class="mb-6">
            <p class="text-gray-600 italic leading-relaxed">
              "{testimonial.content}"
            </p>
          </div>
          
          <div class="flex items-center gap-4">
            <div class="w-14 h-14 bg-gradient-to-r from-primary to-secondary rounded-full flex items-center justify-center text-white font-bold text-lg flex-shrink-0">
              {testimonial.avatar}
            </div>
            <div>
              <h4 class="font-semibold text-gray-900 mb-1">
                {testimonial.author}
              </h4>
              <p class="text-gray-500 text-sm">
                {testimonial.role}
              </p>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

